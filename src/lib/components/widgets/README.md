# 📁 Widgets (`/lib/components/widgets`)

Віджети — це складні UI-компоненти, які відображають стан гри та дозволяють користувачу взаємодіяти з нею. Вони інтегровані в систему `DraggableColumns` та можуть переміщатися користувачем.

## 🧩 Ключові віджети

### `GameInfoWidget.svelte`
Інформаційний центр гри. Відображає текстові підказки про поточний стан:
- "Гра почалась, зробіть хід"
- "VirtualPlayer думає..."
- "Вибрано хід: {direction} на {distance}"
- "Гра завершена"

### `ScorePanelWidget.svelte`
Відображає поточний рахунок гравців, включаючи зафіксовані бали та бали за поточний раунд.

### `ControlsPanelWidget.svelte`
Містить кнопки вибору напрямку та відстані, а також кнопки підтвердження ходу та рестарту.

### `BoardWrapperWidget.svelte`
Огортка для ігрової дошки, що забезпечує її позиціонування та інтеграцію в макет.

## 🏗️ Архітектура
- **SSoT**: Віджети підписуються на глобальні стори (`gameStore`, `boardStore`, `scoreStore`).
- **SoC**: Віджети відповідають лише за відображення. Логіка обробки дій делегується сервісам (наприклад, `userActionService`).
- **Responsive**: Віджети адаптуються до ширини колонок.

## 🔗 Інтеграція
Віджети реєструються у `layoutStore.ts`, що дозволяє зберігати їх положення між сесіями.
