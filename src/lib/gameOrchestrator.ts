// src/lib/gameOrchestrator.ts
import { userActionService } from './services/userActionService';

/**
 * @deprecated Use userActionService instead.
 * This is a temporary export for backward compatibility.
 */
export const gameOrchestrator = userActionService;