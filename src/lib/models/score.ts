export interface FinalScoreDetails {
  baseScore: number;
  totalPenalty: number;
  sizeBonus: number;
  blockModeBonus: number;
  noMovesBonus: number;
  finishBonus: number;
  jumpBonus: number;
  distanceBonus: number;
  totalScore: number;
}
