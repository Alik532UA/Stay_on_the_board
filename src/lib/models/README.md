# 📁 Models (`/lib/models`)

Цей каталог містить **моделі даних** — класи та інтерфейси, що описують структуру основних ігрових сутностей.

## 🏗️ Архітектура фігури (`Piece.ts`)
Фігура в грі — це не просто координати, а складний об'єкт, що відповідає за свій стан та візуалізацію.

### Ключові властивості `Piece`:
- `x, y`: Поточні координати на сітці.
- `type`: Тип фігури (напр., 'standard').
- `color`: Колір гравця, якому належить фігура.

### Логіка руху:
Логіка валідації ходів винесена в `gameLogicService.ts`, але модель `Piece` забезпечує цілісність даних фігури.

## 📊 Стан гри (`gameState.ts`)
Інтерфейси, що описують повний стан ігрової сесії.

- **`GameState`**: Основний інтерфейс стану (позиція, заблоковані клітинки, поточний гравець).
- **`HistoryItem`**: Структура запису в історії ходів.

## 👥 Гравці та Рахунок
- `player.ts`: Модель гравця (ім'я, тип, колір).
- `score.ts`: Структура рахунку (загальний, бали за раунд, штрафи).

## 📝 Конвенції
1. Всі моделі мають бути чітко типізовані (TypeScript).
2. Моделі не повинні містити бізнес-логіки, що змінює стан сторів (SoC).
3. Використовуйте моделі для валідації вхідних даних з Firebase або localStorage.
