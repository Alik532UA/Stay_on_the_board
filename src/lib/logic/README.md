# 📁 Logic (`/lib/logic`)

Цей каталог містить модулі з чистою логікою, яка не залежить від стану Svelte або браузерних API.

## ⚙️ Логіка налаштувань (`settingsLogic.ts`)

Модуль відповідає за валідацію та обробку налаштувань гри.

### Ключові функції:
- **`syncGameMode`**: Автоматично встановлює пресет складності (`Beginner`, `Experienced`, `Pro`) на основі увімкнених функцій (приховування дошки, режим блокування).
- **Валідація**: Перевірка коректності значень перед збереженням у стор.

## 🧪 Тестування
Логіка в цьому каталозі повинна бути покрита модульними тестами (`settingsLogic.test.ts`), оскільки вона не має побічних ефектів.
