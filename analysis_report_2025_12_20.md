# Звіт про аналіз змін (git diff HEAD)

## Оцінка коду

| Критерій | До змін | Після змін | Коментар |
| :--- | :---: | :---: | :--- |
| **Загальна якість коду** | 72 | 88 | Значне покращення структури та чистоти коду, але є зауваження до семантики іконок. |
| **SOLID Принципи** | 65 | 92 | Перехід на компонентну систему іконок та DRY-рефакторинг суттєво покращили архітектуру. |

---

## Аналіз за SOLID принципами

### 1. SRP (Single Responsibility Principle)
- **До:** Компоненти самі відповідали за відображення емодзі як тексту, що змішувало логіку відображення з контентом.
- **Після:** Впроваджено компонент `NotoEmoji`, який взяв на себе всю відповідальність за рендеринг іконок. Це дозволяє легко змінювати стиль (наприклад, з колірних на монохромні) в одному місці.

### 2. OCP (Open/Closed Principle)
- **До:** Для зміни стилю іконок у всьому застосунку потрібно було б редагувати десятки файлів.
- **Після:** Система відкрита для розширення (можна додавати нові іконки через конфіг) та закрита для модифікації (використання компонента не змінюється при зміні провайдера іконок).

### 3. DRY (Don't Repeat Yourself)
- **До:** Дублювання розмітки в таких компонентах як `ThemeDropdown`.
- **Після:** Використання циклів `{#each}` та слотів у `GameModeButton` значно скоротило обсяг коду та полегшило його підтримку.

### 4. LSP (Liskov Substitution Principle) & ISP (Interface Segregation Principle)
- Покращено через чіткішу типізацію та виправлення помилок в інтерфейсах (наприклад, у `online.ts`).

---

## Що погіршило код (Список регресій)

1.  **Семантична неточність іконок**:
    *   Заміна `🏠` (Дім) на `🌍` (Глобус) для посилання на головне меню.
    *   Заміна `🧹` (Мітла) на `🐛` (Жук) для очищення кешу. Це контрінтуїтивно для користувача.
2.  **Залежність від зовнішніх файлів (Broken Assets)**:
    *   Перехід на SVG спричинив помилки 404 (handshake.svg, flag_*.svg), оскільки ці файли або відсутні, або мають неправильні шляхи.
3.  **Регресії в стилізації прапорів**:
    *   Впровадження нових обгорток (`lang-flag-wrapper`) призвело до проблем із закругленням (rounding issues), які раніше були вирішені.
4.  **Помилки в конфігурації**:
    *   Прапор Кримських татар помилково асоційований з Турецьким у деяких частинах системи.

---

## Висновок
З технічної точки зору код став набагато професійнішим, чистішим та легшим для масштабування. Однак, з точки зору UX та цілісності продукту, є критичні зауваження щодо вибору іконок та працездатності графічних ресурсів.

> [!IMPORTANT]
> Рекомендується виправити семантику іконок та забезпечити наявність всіх SVG-файлів у папці `static/emojis/color/` перед фіналізацією змін.
