# План Рефакторингу MainMenu.svelte

**Статус:** Планування  
**Дата:** 2025-12-09  
**Файл:** `src/lib/components/MainMenu.svelte`  
**Розмір:** 22KB (677 рядків)

---

> [!CAUTION]
> **Критичні обмеження:**
> 1. MainMenu — точка входу користувача
> 2. Зміни можуть вплинути на навігацію
> 3. Потребує тестування hotkeys та дропдаунів

---

## Аудит поточного стану

### Статистика
- **Загальний розмір:** 677 рядків, 22KB
- **Script блок:** ~250 рядків
- **Template блок:** ~300 рядків
- **Style блок:** Винесено в `main-menu.css`
- **Outline Items:** 28

### Виявлені проблеми
1. **Великий script блок**: 250 рядків логіки
2. **Багато обробників**: handlePlayVsComputer, handleLocalGame, handleDevMenu тощо
3. **Умовний рендеринг**: Дропдауни, WIP notice, dev menu
4. **Hotkey логіка**: Динамічна реєстрація hotkeys для кнопок

---

## Аналіз функцій

### Категорія 1: Навігація
| Функція | Опис |
|---------|------|
| `navigateTo(route)` | Перехід на сторінку |
| `handlePlayVsComputer()` | Гра проти комп'ютера |
| `handleLocalGame()` | Локальна гра |
| `handlePlayVirtualPlayer()` | Віртуальний гравець |

### Категорія 2: UI елементи
| Функція | Опис |
|---------|------|
| `selectTheme()` | Вибір теми |
| `selectLang()` | Вибір мови |
| `closeDropdowns()` | Закрити дропдауни |

### Категорія 3: Редагування/Dev
| Функція | Опис |
|---------|------|
| `handleDevMenu()` | Dev menu |
| `handleRules()` | Показати правила |
| `handleControls()` | Показати керування |
| `handleSupporters()` | Показати підтримувачів |

---

## Позитивні аспекти

✅ **CSS вже винесено** в `src/lib/css/layouts/main-menu.css`  
✅ **Логіка hotkeys** централізована через `hotkeyService`  
✅ **Компонент працює стабільно**

---

## Запропонована архітектура

### Варіант A: Композиція

```
src/lib/components/
├── MainMenu.svelte              (Каркас)
├── main-menu/
│   ├── TopIcons.svelte          (Тема, мова, donate)
│   ├── GameButtons.svelte       (Кнопки гри)
│   └── DropdownMenus.svelte     (Дропдауни)
```

### Варіант B: Рефакторинг на місці

1. Витягнути handlers в окремий файл `mainMenuHandlers.ts`
2. Зменшити script блок до 100 рядків
3. Залишити шаблон в одному файлі

---

## Рекомендація

**Рекомендую Варіант B:**
1. Мінімальний вплив на шаблон
2. Легше тестувати
3. Script логіка буде в TypeScript файлі (краща типізація)

---

## Чекбокси виконання

### Етап 1: Аналіз залежностей
- [ ] Документувати всі імпорти
- [ ] Виявити залежності handlers

### Етап 2: Винесення handlers
- [ ] Створити `src/lib/components/main-menu/handlers.ts`
- [ ] Перенести функції: handlePlayVsComputer, handleLocalGame, handleDevMenu тощо
- [ ] Імпортувати handlers в MainMenu.svelte

### Етап 3: Верифікація
- [ ] Перевірити всі кнопки меню
- [ ] Перевірити hotkeys
- [ ] Перевірити дропдауни
- [ ] `npm run check` — 0 помилок
- [ ] `npx playwright test` — 22 тести пройшли

---

## Ризики та пом'якшення

| Ризик | Ймовірність | Вплив | Пом'якшення |
|-------|-------------|-------|-------------|
| Зламати hotkeys | Середня | Високий | Ручне тестування кнопок |
| Зламати навігацію | Низька | Високий | Playwright тести |
| Зламати дропдауни | Низька | Середній | Візуальне тестування |

---

## Пріоритет

**Середній** — MainMenu працює стабільно, але винесення handlers покращить читабельність та підтримуваність.
